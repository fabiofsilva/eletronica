language: python
python:
  - "2.7"

install:
  - pip install -r requirements-dev.txt  

before_script:
  - cp .env.example .env

script:
 - flake8
 - python manage.py  test --settings=eletronica.settings.development
